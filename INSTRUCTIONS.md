# Coding Guidelines

- ReactJS application
- Interaction with the backend is done in a service object (see ./src/js/services/UsersService.js as an example)
- CSS Framework is Bootstrap 5

## Design / Theme

- Professional looking with breathing space
- Theme can be found in `docs/design` directory
- Design document for theme is `in docs/design/DESIGN.md`
